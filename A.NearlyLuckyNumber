#include <bits/stdc++.h>
using namespace std;

int main() {
	long long int n;
	cin >> n;
	int rem, lucky = 0;

	while(n != 0) {
		rem = n % 10;
		n = n / 10;
		if(rem == 7 || rem == 4) lucky++;
	}

	if(lucky == 4 || lucky == 7) cout << "YES";
	else cout << "NO";

	return 0;
}
